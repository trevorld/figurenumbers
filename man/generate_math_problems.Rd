% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_math_problems.R
\name{generate_math_problems}
\alias{generate_math_problems}
\title{Generate a numbered sequence of arithmetic problems}
\usage{
generate_math_problems(targets, op = "auto", max_factor = 12L)
}
\arguments{
\item{targets}{Integer vector of desired answers (1-100), in the order
the problems should be numbered.}

\item{op}{One of \code{"auto"} (default), \code{"add"}, \code{"subtract"}, or
\code{"multiply"}.  \code{"auto"} picks subtraction when \code{target < 50},
and otherwise picks addition or multiplication at random
(only considering multiplication when \code{target} actually has
a suitable factor pair, e.g. never for a prime \code{target}).}

\item{max_factor}{Largest multiplication factor to consider.}
}
\value{
A data frame with columns \code{problem} (1-based index), \code{op},
\code{operands} (a list column of numeric vectors, one per problem,
allowing more than two operands per problem), and \code{target}.
}
\description{
\code{generate_math_problems()} generates one problem per target answer,
numbered in order.  This is intended to produce the left-hand
("problems") panel matching a sequence of grid points on the
right-hand panel (see \code{\link[=numberGridGrob]{numberGridGrob()}}).
}
\examples{
set.seed(42)
generate_math_problems(c(34, 37, 67, 64, 34))
}
