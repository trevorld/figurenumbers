% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridsGrob.R
\name{gridsGrob}
\alias{gridsGrob}
\title{Multiple number grids grob}
\usage{
gridsGrob(
  n = 4L,
  ncol = NULL,
  nrow = NULL,
  margin = unit(0.25, "in"),
  r = unit(0.005, "snpc"),
  gp = gpar(col = "black", fill = "black", lwd = 2),
  name = NULL,
  vp = NULL
)
}
\arguments{
\item{n}{Number of number grids to draw.}

\item{ncol}{Number of columns of grids.  The default (\code{NULL}) arranges
the grids as close to a square as possible (\code{ceiling(sqrt(n))}
columns), e.g. 2x2 for 4 grids.}

\item{nrow}{Number of rows of grids.  The default (\code{NULL}) picks enough
rows to fit \code{n} grids into \code{ncol} columns.}

\item{margin}{Printer margin around the whole arrangement (a
\code{\link[grid:unit]{grid::unit()}}).}

\item{r}{Radius of the circles in each grid, passed to \code{\link[=numberGridGrob]{numberGridGrob()}}.}

\item{gp}{A \code{\link[grid:gpar]{grid::gpar()}} object.}

\item{name}{A character identifier (for the grob).}

\item{vp}{A \code{\link[grid:viewport]{grid::viewport()}} object (or \code{NULL}).}
}
\value{
A \code{\link[grid:gTree]{grid::gTree()}} object.
}
\description{
\code{gridsGrob()} arranges several blank \code{\link[=numberGridGrob]{numberGridGrob()}} panels in
reading order in a row/column grid filling a \verb{[0, 1] x [0, 1]} viewport,
inset by \code{margin}.  Intended for printing practice sheets of blank number
grids to hand-sketch candidate puzzle pictures on before encoding them as
\code{segments} in \code{inst/puzzles.yaml}.
}
\examples{
# a two-sided landscape letter PDF of blank grids ready to print (duplex)
# for hand-sketching candidate puzzle pictures
if (isTRUE(capabilities("cairo")) && require("grid")) {
    f <- tempfile(fileext = ".pdf")
    cairo_pdf(f, width = 11, height = 8.5) # landscape letter
    grid.draw(gridsGrob())
    grid.newpage()
    grid.draw(gridsGrob())
    invisible(dev.off())
    unlink(f)
}
}
