% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/puzzleGrob.R
\name{puzzleGrob}
\alias{puzzleGrob}
\title{Puzzle page grob}
\usage{
puzzleGrob(
  segments,
  op = "auto",
  max_factor = 12L,
  margin = unit(0.25, "in"),
  gp = gpar(col = "black", fill = "black", lwd = 2),
  name = NULL,
  vp = NULL
)
}
\arguments{
\item{segments}{A list of numeric vectors of numbers from 1 to 100.
The first element is the (hidden) picture sequence the
math problems are generated from; any later elements
are drawn in the number grid.}

\item{op, max_factor}{Passed to \code{\link[=generate_math_problems]{generate_math_problems()}}.}

\item{margin}{Printer margin around each panel (a \code{\link[grid:unit]{grid::unit()}}).}

\item{gp}{A \code{\link[grid:gpar]{grid::gpar()}} object.}

\item{name}{A character identifier (for the grob).}

\item{vp}{A \code{\link[grid:viewport]{grid::viewport()}} object (or \code{NULL}).}
}
\value{
A \code{\link[grid:gTree]{grid::gTree()}} object.
}
\description{
\code{puzzleGrob()} creates a grid grob of a complete puzzle page:
arithmetic problems (see \code{\link[=generate_math_problems]{generate_math_problems()}}) on the left panel
and the number grid (see \code{\link[=numberGridGrob]{numberGridGrob()}}) on the right panel of a
landscape bifold page (i.e. a landscape sheet folded once vertically
into two facing panels, e.g. two 5.5" x 8.5" panels for letter paper).
}
\details{
The first element of \code{segments} is the picture's point sequence: it is
used to generate one math problem per point (in order) but is \emph{not}
drawn in the number grid -- connecting those points is the puzzle.
Any later elements of \code{segments} are drawn in the grid as pre-drawn
line segments.

The math problems are randomly generated so use \code{\link[=set.seed]{set.seed()}} first
for reproducible pages.
}
\examples{
if (require("grid", quietly = TRUE)) {
    set.seed(42)
    house <- c(63, 67, 37, 15, 33, 63)
    door <- c(65, 45, 46, 66)
    grid.newpage()
    grid.draw(puzzleGrob(list(house, door)))
}
\dontrun{
# a landscape letter PDF ready to print and fold
pdf("puzzle.pdf", width = 11, height = 8.5)
grid::grid.draw(puzzleGrob(list(house, door)))
dev.off()
}
}
