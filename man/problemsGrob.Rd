% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problemsGrob.R
\name{problemsGrob}
\alias{problemsGrob}
\title{Problems panel grob}
\usage{
problemsGrob(
  problems,
  ncol = NULL,
  nrow = NULL,
  gp = gpar(col = "black", lwd = 2),
  name = NULL,
  vp = NULL
)
}
\arguments{
\item{problems}{A data frame as returned by \code{\link[=generate_math_problems]{generate_math_problems()}}
(i.e. with columns \code{problem}, \code{op}, and \code{operands}).}

\item{ncol}{Number of columns of problems.  The default (\code{NULL}) picks
3 columns for up to 18 problems and 4 columns otherwise.}

\item{nrow}{Number of rows of problems.  The default (\code{NULL}) picks
enough rows to fit the problems but at least 6 (so a puzzle
with few problems keeps the same row height instead of
stretching each problem to fill the panel).}

\item{gp}{A \code{\link[grid:gpar]{grid::gpar()}} object.}

\item{name}{A character identifier (for the grob).}

\item{vp}{A \code{\link[grid:viewport]{grid::viewport()}} object (or \code{NULL}).}
}
\value{
A \code{\link[grid:gTree]{grid::gTree()}} object.
}
\description{
\code{problemsGrob()} creates a grid grob laying out arithmetic problems
(as generated by \code{\link[=generate_math_problems]{generate_math_problems()}}) in long-form "stacked"
style: right-aligned operands stacked atop each other, the operation
symbol beside the bottom operand, a rule line underneath, and blank
space below the rule for the answer (plus space above the operands
for carry/borrow marks).  Problems are laid out in reading order in a
column/row grid filling a \verb{[0, 1] x [0, 1]} viewport.
}
\examples{
if (require("grid", quietly = TRUE)) {
    set.seed(42)
    problems <- generate_math_problems(c(34, 37, 67, 64, 34))
    grid.newpage()
    vp <- viewport(width = unit(5, "in"), height = unit(7.5, "in"))
    grid.draw(problemsGrob(problems, vp = vp))
}
}
