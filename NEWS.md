figurenumbers 0.1.0 (development)
=================================

Initial version with the following function:

* `numberGridGrob()` creates a grid grob of a 10x10 grid of (small) circles
  representing the numbers 1 to 100 (in reading order).
